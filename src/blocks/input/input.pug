mixin input(options)
   - const { name, min, max, step, value } = options
   .input
      label(for=`${name}`).input__content=name
      input(
         type="number" 
         id=name 
         name=name 
         min=min 
         max=max 
         step=step 
         value=`${value}`
      ).input__field