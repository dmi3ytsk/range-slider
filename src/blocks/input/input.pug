mixin input(options)
  - const { name="", label="", min="0", step="1" } = options
  .input
    label.input__content=label
    input(
      type="number" 
      id=name 
      name=name 
      min=min 
      step=step 
    ).input__field